version: '3'

services:
  database:
   image: 'postgres:latest'
   restart: always
   
   ports: 
    - 5432:5432
   
   environment:
    POSTGRES_USER: movie
    POSTGRES_PASSWORD: movie
    POSTGRES_DB: movie 
    